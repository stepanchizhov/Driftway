# Routing provider: "mock" (no key needed) or "tomtom"
ROUTING_PROVIDER=tomtom

# Required only when ROUTING_PROVIDER=tomtom
TOMTOM_API_KEY=hq0Up0G1kPN2saF04VtuClEirqohinqx

# Comma-separated list of allowed frontend origins (CORS)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
